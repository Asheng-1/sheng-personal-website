---
import { ScrollReveal } from '@/components/effects/ScrollReveal';
---

<section id="about" class="content-section about" aria-labelledby="about-title">
  <ScrollReveal client:visible className="section-reveal">
    <div class="about__grid">
      <header>
        <p class="section-kicker">ABOUT / 01</p>
        <h2 id="about-title">先真实地学习，再认真地表达。</h2>
      </header>
      <div class="about__copy">
        <p>
          我正在从基础开始认识 AI
          训练工作：读懂规则、拆解需求，也练习判断一段回答是否可靠。
        </p>
        <p>
          这里不包装尚未发生的经历，也不虚构项目成果。现在能诚实展示的，是我正在学习什么、如何练习，以及接下来准备走的路。
        </p>
      </div>
    </div>
  </ScrollReveal>
</section>

<style>
  .about__grid {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(18rem, 1.1fr);
    gap: clamp(2rem, 7vw, 7rem);
    align-items: start;
  }

  h2 {
    max-width: 12ch;
    margin: 0.9rem 0 0;
    font-size: clamp(2.25rem, 5vw, 4.8rem);
    line-height: 1.08;
    letter-spacing: -0.045em;
  }

  .about__copy {
    padding-top: 2.25rem;
    border-top: 1px solid var(--color-border);
  }

  .about__copy p {
    margin: 0;
    color: var(--color-muted);
    font-size: clamp(1rem, 1.5vw, 1.125rem);
    line-height: 1.9;
  }

  .about__copy p + p {
    margin-top: 1rem;
    color: var(--color-text);
  }

  @media (max-width: 53.125rem) {
    .about__grid {
      grid-template-columns: minmax(0, 1fr);
      gap: 1.75rem;
    }

    .about__copy {
      padding-top: 1.5rem;
    }
  }
</style>
